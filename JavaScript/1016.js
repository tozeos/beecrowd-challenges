var input = require("fs").readFileSync("/dev/stdin", "utf8");
var lines = input.split("\n");

// BEE 1042 | DISTÂNCIA

var distance = input;
var time = distance * 2;

console.log(time + " minutos");
